class User {
    constructor(id, firstName, lastName, title, email, avatarUrl) {
        this.id = id
        this.firstName = firstName
        this.lastName = lastName
        this.title = title
        this.email = email
        this.avatarUrl = avatarUrl
    }
}
